{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "python app.py",
  "env": {
    "FLASK_ENV": "production"
  },
  "functions": {
    "api/**/*.py": {
      "runtime": "python-3.9"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.py"
    }
  ]
}
