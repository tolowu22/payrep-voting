Voting App
===========

A tiny Flask web UI for the simple blockchain voting demo.

Quick start (PowerShell):

```powershell
python -m pip install -r voting_app\requirements.txt
python voting_app\app.py
```

Open http://127.0.0.1:5000/ in your browser.
